# A股、港股年报解析 Skill：重新设计方案

设计稿日期：2026-09-05  
状态：供审阅，尚未实施。本文不代表现有 Skill 已具备所列能力。

## 1. 审核结论与建议

保留最初“正文、表格、图表分别处理”的方向，重做解析底座、数据契约和验收方式。现有 V0.1 不适合继续直接叠加财务 JSON 导出：输入给下一层的文字已经出现错序、聚合和遗漏。

原对话确实把 V0.1 限定为“可靠阅读”，把表格结构化留给后续版本。因此，暂未实现表格和图表属于当时的范围取舍；真正需要纠正的是可靠阅读尚未达到、验收结论过宽，以及把证据和质量验证安排得过晚。

建议把这项能力定义为：

> 将指定上市公司报告 PDF 转成有原文证据、可阅读、保留表格结构、部分数据可计算、未解决问题可定位的研究资料包。

Markdown 是阅读输出，表格 JSON 是结构输出，财务事实是经过上下文确认的数据输出。三者从同一份文档结构生成，不相互反推，也不各自形成互不一致的“原文”。

整体采用一个 Skill 入口、多个确定性模块和可替换解析引擎。第一阶段不建设在线平台、全市场数据库或自动投资分析系统。

以下需要直接推翻：

- 以纯文本提取加简单排序作为通用版面解析底座。
- 把证据绑定和验证推迟到 V0.3。
- 把不同财务标签简单合并成一个“标准指标”。
- 使用未经标注样本校准的 0.98、0.999 作为入库依据。
- 把“会计关系能对上”当作识别正确的证明。
- 承诺“随便一份 A 股年报都稳定解析”，却只测试一份报告的页码锚点。

需要保留：

- 原 PDF 保留、物理页溯源、按内容类型分流。
- 复杂表格不硬压成 Markdown 管道表。
- 原值、单位、期间、脚注和不同版本并存。
- 解析与研究、预测、写作分开。

## 2. 本次对 V0.1 的实证审查

审查对象是当前本地安装的 [解析脚本](/Users/eric/.codex/skills/annual-report-markdown/scripts/parse_annual_report.py) 及其已生成的 [茅台解析底稿](/Users/eric/Documents/Codex/探索skill-1/600519_贵州茅台_2026年半年度报告_markdown/report.md)。同时回读了最初对话的完整设计文本。

对本地茅台 PDF 的物理第 2、5、8、26 页进行了渲染核对；港股需求另参考官方规范和真实报告。此次没有修改 Skill、原 PDF 或现有解析产物，也没有安装新解析引擎。

| 项目 | 已确认的实际问题 | 对设计的影响 |
|---|---|---|
| 正文顺序 | 原 PDF 第 2 页“一”到“十一”顺序正常，Markdown 从“十一”倒排到“一”，“重要提示”落到最后 | 坐标必须经过变换、旋转和原点统一后才能参与排序 |
| 文字定位 | 多个片段被当作同处在坐标 (0,0)，并入页尾长行，标题、数字、勾选状态与正文分离 | 需要可信的字符/词级几何证据，不能依赖任意文本片段的起点 |
| 版面误判 | 第 8 页为全宽正文和三张表，被标成双栏 | 先识别表格区域，再判断正文栏结构 |
| 内容遗漏 | 第 8 页一条含 1,085 字符的聚合行在双栏处理时被漏掉，其中包含多项金额 | 每一步必须记录输入对象去向，不能只检查最终文本是否非空 |
| 页眉清理 | “合计”进入重复页眉候选，108 条独立“合计”在 48 页中被删除 | 按位置和上下文分类，原始证据层保留全部对象 |
| 章节地图 | 同页多节只留一个，漏第一节和第七节；多数条目只剩“第几节”，没有章节名称 | 章节边界定位到块，支持同页多节及层级结构 |
| 质量报告 | 倒序页没有告警；大量漏字页仍非稀疏页 | 页码齐全、字数阈值不能代表内容可靠 |
| 内容类型 | 当前只输出纯文本及两个索引文件，没有表格单元格、图像或脚注关联 | 需要统一的结构表示后再分流导出 |

代码定位：坐标处理在 [第 47 行](/Users/eric/.codex/skills/annual-report-markdown/scripts/parse_annual_report.py:47)，双栏分支在 [第 76 行](/Users/eric/.codex/skills/annual-report-markdown/scripts/parse_annual_report.py:76)，页眉识别与删除在 [第 90 行](/Users/eric/.codex/skills/annual-report-markdown/scripts/parse_annual_report.py:90)，同页章节处理在 [第 169 行](/Users/eric/.codex/skills/annual-report-markdown/scripts/parse_annual_report.py:169)。

pypdf 官方文档明确区分文本矩阵与页面变换，并提示复杂文档中的坐标定位限制；它本身也不承担 OCR 和文档版面语义识别。问题出在当前用法与职责安排，不能据此判断 pypdf 对所有用途都不可用。[pypdf 文本提取文档](https://pypdf.readthedocs.io/en/stable/user/extract-text.html)

因此，之前“110 页锚点齐全”的检查只能证明分页标记存在。现有茅台 Markdown 应视为未通过内容验收的旧产物，不能据其制作可信财务数据。如何替换它属于后续实施，本次不执行。

## 3. 目标范围：怎样理解覆盖大部分 A/H 年报

覆盖要拆成三个维度，分别报告，不能用一个“成功率”混在一起。

1. **内容留存覆盖**：报告里的正文、表、图、附注是否都有记录、证据或明确未解析状态。
2. **结构恢复覆盖**：多少段落、表格、跨页关系及章节边界已可靠恢复。
3. **可计算数据覆盖**：多少财务值的文字、行列、期间、主体、单位及口径已经确认。

例如，一张复杂表以原图保留且标为待复核，可以计入内容留存，不能计入已结构化或可计算覆盖。

目标输入：

- A 股、港股完整年度报告；兼容中期、半年度和季度报告。
- 简体中文、繁体中文、英文，以及同文档中的中英混排。
- 原生文字、扫描、混合 PDF，含已有但可能不可靠的 OCR 文字层。
- 常见工业、消费、科技、资源、地产、银行、保险、证券与综合企业。
- 单栏、多栏、同页多种布局、横向页面、跨页表格及图文混排。

不承诺首版完全自动解决：

- 重度模糊、缺页、损坏、手写或无法辨认的原文。
- 任意图表的精确数值反演。
- 所有行业指标的一次性统一。
- 公告、招股书、调研纪要和多年知识库的全面处理。
- 从 PDF 内容本身证明披露真实、审计无误或文件来源权威。

这些情况仍应有清楚的状态和证据去向。可解密但需要密码的文件报告原因，输入损坏时保留失败记录；不把“未成功解析”改写成“未披露”。

## 4. 新的整体架构

~~~text
指定 PDF
   │
   ▼
来源登记与体检
文件哈希、报告身份、页面几何、语言、文字层质量
   │
   ▼
页面及区域识别
正文 / 表格 / 图表 / 脚注 / 页眉页脚 / 未知区域
   │
   ▼
按区域选择解析方法
可信原生文字 / OCR / 表格结构识别 / 图像裁剪
   │
   ▼
统一文档结构
页、块、单元格、文字、坐标、阅读顺序、来源、候选结果
   │
   ├── 正文与章节 ───────────────→ Markdown
   ├── 物理表格 → 跨页逻辑表 ───→ 表格 JSON + HTML
   ├── 图表与脚注 ───────────────→ 图片 + 说明 + 可核对标签
   └── 上下文明确的表格/文字 ───→ 财务事实候选
                                          │
                                   语义与数值验证
                                          │
                                  可计算 JSON / CSV

证据记录和分层质量检查贯穿每一步
遇到冲突 → 局部重试 / 保留候选 / 待复核，不悄悄补齐
~~~

这里新增的关键部件是“统一文档结构”。它保存文件中出现了什么、位于哪里、与什么相连，而不是先决定最后 Markdown 应该长什么样。

证据也不再是处理结束后补写的一个 Metadata 分支：一开始就给页面和对象建立标识，此后重排、合并、规范化、导出都沿用这些来源关系。

## 5. 各层职责与数据边界

| 层 | 保存什么 | 可以做什么 | 不能做什么 |
|---|---|---|---|
| 原始证据 | PDF 哈希、页图、原生对象、引擎原始识别结果 | 原样保存、添加来源信息 | 用后续“修正值”覆盖原始观察结果 |
| 文档结构 | 段落、栏、表格网格、图片、脚注、层级、阅读顺序 | 基于证据建立关系，保留候选与不确定性 | 因识别不了就无声删除 |
| 财务语义 | 指标候选、期间、主体、币种、单位、准则、重述状态 | 明确上下文后映射、定点换算 | 按相似名称把不同口径强行合并 |
| 阅读与数据输出 | Markdown、HTML、JSON、CSV、复核入口 | 根据已选定的结构生成一致视图 | 在导出时另用模型重写一份“原文” |
| 上层研究 | 摘要、比较、分析、写作 | 使用质量状态明确的数据 | 把未验证候选当已确认事实 |

“原始文字”指解析引擎的原始观察，不代表天然正确。OCR 原始值也可能错，最终证据仍是指定 PDF 及其可见页面。

基础层完整登记内容。哪些内容对某篇文章重要、哪些表格值得浓缩，是上层研究层的选择，不在解析阶段替用户删掉治理、风险、附注或非财务数据。

## 6. 来源登记与文档体检

### 6.1 报告身份

不再只从“六位代码_公司_年份”文件名推断身份。文件名是线索，封面、重要提示、财务报表日期和编制基础是核对来源。

建议保存：

- 文件身份：document_id、PDF SHA-256、文件名、原路径、页数、文件大小、已知来源 URL。
- 发行人身份：原文公司名、辅助简称、issuer_id、交易所和证券代码列表。
- 报告身份：报告类型、期间起止、财年标签、发布日期、文件语言。
- 版本身份：原版/更正/补充、与另一文件的关系、是否中英对应版本。
- 会计信息：报告明示的准则、列报币种；审计或审阅状态另绑定适用章节/报表。
- 核实信息：每个身份字段的来源块及状态；冲突时保留候选，不取文件名强行覆盖正文。

A/H 代码、中英版本和不同交易柜台不能各自误建为不同公司，也不能合并成同一个文件。AIA 2024 年报封面即列出多个交易柜台代码，是“发行人—证券标识—文件版本”需要分层的实际例子。[AIA 2024 年报](https://www.aia.com/content/dam/group-wise/en/docs/investor-relations/2025/2024%20Annual%20Report%20(Eng).pdf)

### 6.2 页与区域体检

每页检查文字数量、文字分布、图片区域、字符异常、旋转、裁切框、字体编码异常和对象重叠情况。既不能把低字数封面判作失败，也不能把已有隐藏 OCR 层的扫描页判作可靠原生页。

判断以区域为单位：

- 可信原生文字区域：直接取文字和坐标。
- 图片正文区域：OCR，同时保留区域图。
- 有文字但乱码、缺字或位置异常区域：原生提取与 OCR 分别形成候选。
- 全页有文字但局部表格为图片：只对图片区域补 OCR。
- 空白、照片或装饰页：记录内容类型，不伪造正文。
- 无法分类区域：保留截图与待识别状态。

全篇做廉价体检；复杂模型按需要运行。首轮每页保留可再现的渲染参数，必要的证据图持久保存；其余全页图可按页生成并缓存。

## 7. 几何、版面和统一文档结构

### 7.1 坐标统一

内部约定：

- physical_page 从 1 开始，与原 PDF 物理页一一对应。
- 原页面保留 MediaBox、CropBox、rotation 和 PDF 坐标信息。
- 工作坐标统一为“旋转和裁切后的可见页面，左上角原点，单位 point”。
- 保存原坐标到工作坐标、工作坐标到渲染像素的变换及 DPI。
- 截图上的框必须能反算回原 PDF，不能只保留模型的归一化框。
- 印刷页码另存 printed_page_label，可以是罗马数字、附录号或空值。
- 一个物理页若包含两个印刷页，保存两个子区域，不伪造两个原始物理页。

不同工具可能用不同起点、单位、页号基数。例如 MinerU 的部分输出使用从 0 开始的 page_idx 与 0–1000 归一化 bbox，必须通过适配器转换。[MinerU 输出格式](https://opendatalab.github.io/MinerU/reference/output_files/)

### 7.2 页面内结构

先识别区域，再处理区域内部顺序。一个页面可以同时有跨栏标题、左右正文、全宽表格和页底脚注，不能给全页贴一个“双栏”标签就把所有内容分成两半。

正文阅读顺序和表格读取方式分开：

- 正文按栏和段落的关系建立顺序。
- 表格保留行列网格，不把列当成正文栏。
- 同页并排中英内容先标语言与对应关系，不直接拼成前后两段。
- 跨栏标题在它所属的内容前出现；浮动图片不切断一个完整段落。
- 签名、勾选框、编号列表、边注、表注和普通脚注使用单独类型。
- 对重复的隐藏文字层只做有证据的去重，保留被排除对象与原因。

### 7.3 最小对象模型

~~~text
Document
 ├── Page
 │    ├── TextBlock / Heading / List / Footnote
 │    ├── TableFragment
 │    │    └── Cell
 │    ├── Figure
 │    └── UnresolvedRegion
 ├── SectionTree
 ├── LogicalTable
 ├── Observation / FactCandidate / AcceptedFact
 └── Evidence / Issue / ProcessingEvent
~~~

共同字段至少包括对象 ID、所属文档、页及区域、对象类型、原始文字/对象引用、解析方法、模型或程序版本、来源粒度、候选状态。

对象关系至少包括 belongs_to、reading_before、continues、header_for、footnote_of、translated_counterpart 和 derived_from。这些关系用 JSON ID 引用即可，不需要第一版引入图数据库。

每个源对象的去向属于以下之一：进入正文、进入表格、进入图表、归入页眉页脚、作为重复对象保留记录、未解决。这个账目可以发现程序内部漏对象；它不能单独证明检测器发现了所有可见区域，还需要图像侧覆盖检查和标注样本验收。

## 8. 正文、章节、脚注：怎样生成好用的 Markdown

### 8.1 章节地图

综合使用 PDF 书签、目录页、实际标题、字体和版面层级。书签和目录都是候选，需要与正文位置核对，不能无条件相信。

保留两套信息：

- 原始目录：如“第三节 管理层讨论与分析”“Financial Review”，原文照存。
- 标准章节标签：如 management_discussion、financial_statements、notes、governance，用于检索和路由。

边界保存为起始块和结束块，物理页范围由块位置推导。支持同页两个章节、标题跨两行、无编号标题和跨页段落。不能再用“下节起始页减一”处理所有情况。

识别到某附注引用却未找到正文时，应返回“引用未解决”，不能默认为附注不存在。

A 股格式规范本身也有修订和适用性要求，章节标签应可配置，不能把某一年的章节序号写死。[证监会 2025 年年度报告格式准则及附件](https://www.csrc.gov.cn/csrc/c101954/c7547588/content.shtml)

### 8.2 正文整理

合并同段换行、恢复列表和标题，但保存可回查的块来源。数字、否定词、范围词、时间限定、公司判断语气不改写。

繁简转换、英文术语翻译、全半角统一及搜索别名放到辅助检索字段，不覆盖原文。英语断词连字修复必须有词和位置依据，不能把负号、减号或财务符号当作连字符消除。

页眉页脚分类后从阅读正文中隐藏；原始层仍保留它们。删除条件依赖区域、重复模式及字体等证据，不以“出现三次”作为充分条件，尤其不能全篇删除“合计”等通用词。

### 8.3 脚注与限定条件

正文脚注、表下注释、财务报表附注编号是不同类型。编号可能在每页重启，“附注 5”也可能属于不同主体，绑定时必须带章节上下文。

表格或数字的证据应包含必要脚注，尤其是“不含税”“经重述”“持续经营”“不含某业务”“按固定汇率”“同比口径调整”等限定。

Markdown 内的简单表格可内嵌管道表；复杂表格在原位置给出表名、简短状态、预览和 HTML/JSON 链接。已完成的结构化结果不应在 Markdown 中退化成一串无法对应的数字。

## 9. 表格：结构恢复是核心工程

### 9.1 不只识别三大表

先建立全书表格清单，再区分主表、附注表、经营数据表、治理表、名单表等。所有表都留存，主表和高复用附注优先进入财务语义整理。

主表类型至少包括资产负债/财务状况、损益、其他综合收益、权益变动、现金流量；损益和综合收益可以合并或分列。可能同时有合并与公司报表，也可能有额外比较时点。IAS 1 的完整财务报表定义明确包含这些形式，不能把港股全部压成固定三张表。[IAS 1 官方说明](https://www.ifrs.org/issued-standards/list-of-standards/ias-1-presentation-of-financial-statements/)

### 9.2 表格处理顺序

1. 定位表格与外部标题、币种单位、注释区域。
2. 识别格线、无框线列边界、行边界及合并单元格。
3. 把可信原生文字按坐标放回单元格；扫描表使用 OCR 文字候选。
4. 保留多级表头、缩进行名、行列跨度和空白占位。
5. 识别数据区、表头区、行标题区和附注编号列。
6. 建立单元格到表头、行名、单位、期间、注释的关系。
7. 判断跨页关系，再形成逻辑表。
8. 结构通过检查后，生成阅读表和语义候选。

结构模型负责候选网格，文字识别负责候选字串。二者对不上时记录冲突，不能为了装进格子而截断文字，也不能把模型生成的数字替换原生文字后当作原文。

### 9.3 复杂结构

需要处理的结构包括：

- 多级列头、跨行行名、纵向及横向合并单元格。
- 单位放在表外、放在行内、不同列使用不同单位。
- 同一表内从“本期/上期”切换到“期末/期初”的中途表头。
- 无框线财务表、粗细线混用、底纹分组。
- 带“其中”的上下级行，缩进表示层级但不代表所有子项可直接求和。
- 空白、短横线、负数、括号负数、脚注上标和非数字列。
- 表中表、并排独立表、宽表横向拆页、单行文字跨页。
- 注释嵌在表尾、表头在上一页而续页没有重复表头。

### 9.4 跨页表格

区分“某页上的物理表片段”和“跨页逻辑表”。原始片段不因拼接而消失。

拼接依据包括页面相邻关系、表名和续表提示、列几何、表头路径、期间、币种、主体、注释编号、行项目延续和表尾特征。

以下情况不能仅凭同标题拼接：

- 下一页同名表切换了年度或主体。
- 合并报表结束后接母公司报表。
- 同一附注按产品、计量模型或币种分成多个子表。
- 下一页是另一期间的完整表，即使也写“continued”。

同一逻辑表允许有不同期间的子组，但必须明确建模子组边界；不能直接把它们合成一张共用同一表头的矩形表。

重叠行名只能用于辅助判断，不能据此删掉看似重复的业务数据。重复表头可以从阅读版隐藏，源片段和继承关系仍保留。

### 9.5 保存格式

| 输出 | 用途 | 必须保留 |
|---|---|---|
| 表格 JSON | 表格结构的主要表示 | 单元格 ID、行列范围、rowspan/colspan、原值、来源、表头关系、分页片段、状态 |
| 表格 HTML | 人工阅读和复核 | 原始表头、合并关系、单位、脚注、来源链接 |
| 原图裁剪 | 对照证据 | 标题、完整表体、单位及相关表注；跨页表有多张原片段 |
| CSV | 下游计算的派生视图 | 已明确展开规则的列、原值/标准值和来源 ID |

CSV 默认优先使用“每条观察值一行”的长表，不用它保存原始合并结构。宽表 CSV 仅对已核实列关系的表导出，并附字段与来源映射。复杂表不能把 JSON 丢掉只留下 CSV。

表格自身可以结构清楚但语义尚未归类。此时仍能交付原样 HTML/JSON，只是不进入标准指标数据集。

## 10. 图表、图片和图表理解

图片对象提取不等于图表提取：柱线图可能由矢量线条和文本组成，整页也可能只有一张大背景图。因此通过版面区域识别图表，再按区域渲染证据，必要时另存原始嵌入图像。

每个图表保存：

- 图表区域、页码、标题、图例、轴、单位、脚注。
- 类型，如柱形图、折线图、饼图、流程图、地图、照片。
- 可见标签的文字与位置。
- 与正文、表格及脚注的关系。
- 描述内容的生成方法、证据和核对状态。

图表理解分三档：

1. **原图保留**：完整保存可见区域，允许说明尚未解读。
2. **可核对描述与标签**：识别图的主题、系列、期间、轴单位和明确印出的数值；描述不能擅自给原因或推导投资结论。
3. **估算数据**：由柱高、折线位置反推数值属于估算，仅在用户明确需要时生成，单独标识，不进入已验证财务事实。

所有图表先完成清单与证据留存；数据图优先处理标题、标签和脚注。精细描述可按需求和成本选择，未完成的图表必须能在入口看到状态，不能宣称已经实现全面图表理解。

五年趋势图不必然具有五年可比口径。AIA 2024 年报的五年信息包含会计口径变化说明，后续生成序列时必须携带脚注和适用期间。[AIA 2024 年报](https://www.aia.com/content/dam/group-wise/en/docs/investor-relations/2025/2024%20Annual%20Report%20(Eng).pdf)

## 11. 财务语义：先保住口径，再讨论标准化

### 11.1 指标名字不能直接作为唯一键

“营业收入”“营业总收入”“主营业务收入”不是无条件同义词；“净利润”“归母净利润”“经调整净利润”也不能合并。

为每个值保留以下维度：

| 维度 | 例子与作用 |
|---|---|
| 原始标签与概念映射 | 原文照存；标准概念可以未确认，不能强制填满 |
| 主体 | 集团合并、母公司、子公司、分部；不由章节距离随意继承 |
| 期间类型 | instant 表示某日，duration 表示一段期间 |
| 具体期间 | 开始日、结束日或时点；年度标签只是显示辅助 |
| 比较状态 | 本期、上期、期初、期末、调整前、重述后 |
| 披露版本 | 来自哪份报告、何时披露、是否修订；历史期间可有多个版本 |
| 币种与倍率 | HKD/CNY/USD；元、千元、百万元、亿元分别记录 |
| 数量类型 | 金额、百分比、百分点、每股金额、股数、吨数、人数等 |
| 会计与计量口径 | CAS、HKFRS、IFRS、US GAAP 或报告明示其他准则；法定/调整后/固定汇率等 |
| 业务维度 | 产品、地区、业务、渠道、分部、合同类别等 |
| 适用范围 | 持续经营、终止经营、是否含税、是否含特定业务等 |
| 审计/审阅状态 | 绑定具体报表或信息，不给整本年报一概赋值 |

同一个 2024 年数字可能来自 2024 年年报原披露，也可能来自 2025 年年报重述比较列。它们是不同观察值，不能覆盖。对比时由调用方选择“最新重述口径”或“当时披露口径”，并记录选择。

财年也不必在 12 月结束。联想官方财务页面以 3 月 31 日为年结并使用千美元列报，说明不能从“2025 年”推断自然年和人民币。[联想财务状况表](https://investor.lenovo.com/en/financial/balance_sheet.php)

### 11.2 币种和单位

列报币种、功能币、外币风险表里的币种维度分别保存，不能把看到的第一个币种套用全表。IAS 21 明确区分功能币与列报币，两者可能不同。[IAS 21 官方说明](https://www.ifrs.org/issued-standards/list-of-standards/ias-21-the-effects-of-changes-in-foreign-exchange-rates/)

单位可以从文档、章节、表格继承，也可在行、列或单元格局部覆盖；每次继承都要保存来源。行与列的局部声明若冲突，不能任意决定谁优先，应保留候选并复核。

以十进制定点方式解析和换算，保留：

- raw_value：原始字串。
- numeric_value：语义明确后的十进制数，序列化为字符串，避免浮点精度损失。
- normalized_value：单位倍率换算后的数，仍为十进制字符串。
- transformation：换算倍率、规则版本与输入来源。
- display_value：下游展示时生成，不作为新的事实值或核验依据。

单位倍率换算不等于币种换算。本 Skill 默认不引入市场汇率把所有数据换成人民币；跨币种分析交由有明确汇率和日期依据的上层任务。

### 11.3 空值、符号和比率

区分明确 0、空白、短横线、未披露、不适用、无法识别、约数、范围和“小于某值”。保留原记号及其说明；未证实的空值不补 0。

括号负数与脚注括号分开；百分比、百分比变动和百分点变动分开；基本与稀释每股收益分开。中国报表中的“损失以负号填列”等规则绑定适用字段，不做全局符号翻转。

### 11.4 一个事实候选的示意

以下为字段结构示例，数值和日期均为虚构，不是本地茅台数据；仅展示关键字段，不是完整接口 Schema。

~~~json
{
  "observation_id": "docA/table004/cell032",
  "source_kind": "table_cell",
  "original_label": "Revenue",
  "concept_mapping": {"concept": "revenue", "status": "needs_review"},
  "raw_value": "1,234.50",
  "numeric_value": "1234.50",
  "currency": "HKD",
  "scale": "1000",
  "normalized_value": "1234500.00",
  "period": {
    "kind": "duration",
    "start": "2024-04-01",
    "end": "2025-03-31"
  },
  "entity_scope": "consolidated",
  "accounting_basis": "HKFRS",
  "restatement_status": "as_reported",
  "evidence_refs": {
    "value": ["ev-cell032"],
    "row_label": ["ev-row07"],
    "period": ["ev-header2025"],
    "unit_currency": ["ev-unit004"],
    "entity_scope": ["ev-title004"],
    "accounting_basis": ["ev-basis01"]
  },
  "quality": {
    "text": "checked",
    "structure": "needs_review",
    "semantics": "needs_review",
    "normalization": "computed_from_candidate",
    "eligible_for_calculation": false
  }
}
~~~

允许候选层保留试算值，但结构或语义未通过时，不能流入正式可计算数据。实际实现需为必要上下文字段补齐证据；单位未知时 normalized_value 必须为空，而不是猜倍率。

### 11.5 行业与准则扩展

采用共享解析器加行业语义配置：

- 银行：利息收入/净利息收入、手续费、贷款分级、ECL 阶段、资本和流动性指标。
- 保险：保费、保险收入、保险服务结果、再保险、CSM、内含价值及新业务价值分开。
- 证券：客户资金、代理买卖证券款、回购、资管与净资本；注意含/不含客户资金的口径。
- 工业、资源、消费：分产品/地区/渠道、产销量、产能、价格、成本及存货。
- 地产、建筑：合同销售与确认收入、面积、合同资产负债、项目进度。
- 科技、医药：研发费用与资本化、活跃用户、订单、授权里程碑等报告明示指标。

行业标签帮助选择解释规则，不能成为忽略其他内容的过滤条件。综合企业允许同一报告使用多个行业配置。

准则与词典应有版本及适用期间。IFRS 18 自 2027 年 1 月 1 日或以后开始的年度期间生效，允许提前采用，将替代 IAS 1；因此新设计必须容纳列报结构变化，不把当前科目表永久写死。是否适用某报告仍看其声明。[IFRS 18 官方说明](https://www.ifrs.org/issued-standards/list-of-standards/ifrs-18-presentation-and-disclosure-in-financial-statements/)

## 12. 证据：从“第几页”推进到“为什么属于这个字段”

每条可计算数据的证据应能够回答：

1. 原值在哪一页、哪个表格单元格、正文片段或图中标签？
2. 表格的行名列头，或正文的完整语句，或图表的系列和坐标轴在哪里？
3. 单位、期间、主体及重述状态从哪里取得？
4. 有哪些影响理解的脚注？
5. 哪个引擎识别，做过什么处理，因何选择该候选？

事实来源明确区分 table_cell、text_span 和 figure_label。正文事实需要完整语句及限定条件；图表标签需要系列、期间、图例、轴单位及脚注。三类来源采用同一套语义和质量门槛，不能为了统一格式把正文或图表数字虚构成表格单元格，也不能让它们绕过验证。

证据粒度分 page、region、table、cell、span。只有表级框时就报告 table，不编造单元格坐标；上下文不能唯一确认的数值不能被标成可计算。

证据可跨页：值在续页，表头和单位在前页，编制基础在另一章节。必须保存这些来源组成的证据集合。

对正文合并、表格拼接、重复表头隐藏、单位换算和人工修正，记录输入对象、操作、输出对象和规则版本。人工核对后的修正另存修订记录，保留旧候选；后续重跑识别结果不能抹掉人工决定。

建议提供轻量本地复核页：左侧原 PDF 区域，右侧识别表和字段上下文，直接显示冲突。第一版可以生成可点击的静态复核页，后续才考虑交互式修改界面。

## 13. 质量体系：解析完成与内容可信分别报告

### 13.1 五层检查

| 层次 | 检查内容 | 能证明的范围 |
|---|---|---|
| 运行与文件 | 页数、哈希、文件完整、任务完成或失败 | 程序处理过哪些输入 |
| 内容覆盖 | 检出对象去向、图像区域覆盖、缺表和重复检测 | 已知内容是否在处理时丢失；仍需防检测漏项 |
| 结构 | 阅读顺序、表头单元格关系、跨页连接、章节边界 | 内容之间的关系是否符合证据 |
| 文字与语义 | 原值、符号、期间、币种、主体、口径 | 一个值是否能被正确解释 |
| 财务一致性 | 合计、勾稽、跨表或跨期关系 | 已解释字段是否存在应调查的冲突 |

对关键数字的核对，使用独立原文标注或视觉复核建立真值。第二引擎与第一引擎一致只是一个信号：它们可能共享模型或犯相同错误。

### 13.2 财务校验必须先判断适用性

| 校验 | 适用前提与限制 |
|---|---|
| 资产 = 负债 + 权益 | 同主体、同日、同币种、同单位、同重述状态；允许与原列报精度相符的舍入差 |
| 利润总额 − 所得税 = 净利润 | 必须对应同一报表定义及范围；区分终止经营和其他单列项目 |
| 现金流变动与期初期末 | 必须包含报告列示的相关调节项，不能只固定拼三个净现金流 |
| 分项合计 | 仅适用于完整、互斥且声明覆盖总额的项目；“其中”常是不完全列举 |
| 分部合计 | 识别内部交易抵销、未分配项目、分部计量口径及调节表 |
| 跨表现金对照 | 财务状况表现金与现金流量表口径可能有调节，不能一律要求直接相等 |
| 同比重算 | 先确认期间和重述口径；基期为零、负数、固定汇率或报告另有定义时，不套一个通用公式 |
| 期初与上期期末 | 先识别重述、准则变更和合并范围变化；且仅在输入包含所需报告时执行 |

真实反例：AIA 2024 年报现金流量表在印刷第 171 页（PDF 物理第 173 页）列有银行透支调节，所以不能把它与财务状况表的现金数字简单做硬等式。本例由年报文本核对得到，未作为本次视觉验收样本。[AIA 2024 年报](https://www.aia.com/content/dam/group-wise/en/docs/investor-relations/2025/2024%20Annual%20Report%20(Eng).pdf)

误差容忍来自各输入和合计的列报精度，可用舍入区间求和判断，不默认设成任意 0.1% 或 1%。校验结果包括通过、未通过、不适用和条件不足，并保存实际输入与差额。

任何关系失配都先形成问题，不能反向修改金额来“配平”。关系全部通过，也不能证明没有整列对调、同期误配或单位整体错用。

### 13.3 对象状态与使用资格

建议对象状态：已提取、待复核、按规则验收通过、解析失败；另记是否人工核对。

使用资格拆开记录：

- readable：可以用于阅读，但保留对应警告。
- traceable：能够定位原 PDF，不表示转录或含义已正确。
- citable：引文文字、必要限定和上下文已经按相应规则核对，可带证据引用。
- computable：文字、结构、上下文、单位和必要证据均满足规则。

错误 OCR 即使有准确页框也只满足可定位，不能作为公司原话引用。未核实区域可作为待复核原图展示，不给其文字转录授予可引用资格。

表格可读不等于所有单元格可计算。文件可以部分成功，已通过的表不被无关扫描页阻塞；但存在未解决的重要主表时，整份报告不能标为“财务结构化完整”。

引擎原始置信分与“文字、结构、语义、证据、计算”的状态分别保存。不把模型分数解释成真实正确概率；将来只有在代表性标注集上完成校准后，才考虑概率阈值。

### 13.4 复核队列

按问题的实际影响排序：关键主表及单位期间冲突优先，其次高复用附注、正文重要限定、其他版面问题。每条包括对象、原因、证据裁剪、候选、已试方法和下一步。

单区域采用有界升级：主方法一次；必要时替代方法一次；仍冲突则留待复核。允许为明确可恢复错误调整一次参数，但不无限重跑，也不为了“全部通过”持续调用大模型。

## 14. 引擎选型：复用成熟能力，自建财报约束

建议先以 Docling 标准管线作为主干候选，搭配原生 PDF 几何/文字证据；PP-StructureV3 作为中文 OCR 和复杂表格的候选补充；MinerU 作为对照方案。最终主引擎由真实年报测试决定，第一版生产运行不要求三套模型同时常驻。

| 候选 | 推荐职责 | 已知边界 |
|---|---|---|
| PDF 渲染器 + pdfplumber | 页面证据、字符与线条位置、原生表格候选 | 不自带 OCR，也不负责财务口径 |
| Docling 标准管线 | 版面、阅读顺序、表格、图片和结构化文档表示 | 需验证本机性能、繁体和复杂财报；每个单元格不保证都有细粒度坐标 |
| PP-StructureV3 | 中文扫描及复杂区域的 OCR、表格/版面候选 | 后端、模型和依赖需实测；通用榜单不能代替年报验收 |
| MinerU | 全流程对照，必要时替换主方法或处理困难页 | 输出需适配坐标、验证跨页关系；版本与许可不能沿用旧印象 |
| PyMuPDF | 可选的渲染、文字/表格提取通道 | 使用前确认 AGPL 或商业许可适用方式，不作为必须依赖 |

能力依据：[pdfplumber 官方仓库](https://github.com/jsvine/pdfplumber)、[Docling 文档模型](https://docling-project.github.io/docling/concepts/docling_document/)、[PP-StructureV3 输出说明](https://github.com/PaddlePaddle/PaddleOCR/blob/main/docs/version3.x/pipeline_usage/PP-StructureV3.en.md)、[MinerU 官方仓库](https://github.com/opendatalab/MinerU)、[PyMuPDF 文档与许可](https://pymupdf.readthedocs.io/en/latest/about.html#license-and-copyright)。

原生提取结果、布局模型结果与财务约束的融合由我们控制。不要把任何工具输出的 Markdown 当成权威中间层，也不要从其 Markdown 重新猜表格。

Mac 上采用独立运行环境，登记 Python、引擎、模型、硬件后端和内存需求；优先测试可运行的标准/CPU管线，不假设所有 CUDA 或 MPS 配置都能直接使用。大批量吞吐待实测后再考虑其他计算设备。

默认本地处理；下载模型和上传报告是两回事。Docling 提供离线模型路径和远程服务开关，方案采用关闭远程服务的默认配置。商业 OCR 或远程视觉模型仅作为明确配置的可选方式，展示上传范围和费用依据。[Docling 离线与远程配置](https://docling-project.github.io/docling/usage/advanced_options/)

固定依赖与模型版本，记录代码和模型许可。Docling 代码为 MIT、PaddleOCR 代码为 Apache 2.0；模型另核。MinerU 当前许可证包含附加条款，实施时读取所选版本，不依赖旧版“AGPL”的印象。[Docling 仓库](https://github.com/docling-project/docling)、[PaddleOCR 许可证](https://github.com/PaddlePaddle/PaddleOCR/blob/main/LICENSE)、[MinerU 许可证](https://github.com/opendatalab/MinerU/blob/master/LICENSE.md)

## 15. 给用户的输出与使用体验

默认在原 PDF 平级建立一个新的研究包，原 PDF 作为外部只读来源并记录路径及哈希。需要可搬移的离线包时，可选择把 PDF 复制进包中；复制不作为默认重复备份行为。

建议目录：

~~~text
报告名_研究包/
├── 00-阅读入口.md
├── 正文/
│   ├── 01-公司信息.md
│   ├── 02-经营回顾.md
│   └── ……按实际章节生成
├── 表格/
│   ├── index.json
│   ├── table-0001.json
│   ├── table-0001.html
│   └── ……含必要原图；CSV按需导出
├── 图表/
│   ├── index.json
│   ├── figure-0001.png
│   └── figure-0001.json
├── 数据/
│   ├── facts.jsonl
│   └── candidates.jsonl
├── 索引/
│   ├── manifest.json
│   ├── document_map.json
│   ├── evidence.jsonl
│   ├── quality.json
│   └── review_queue.jsonl
├── 复核/
│   └── index.html
└── _internal/
    ├── run.json
    ├── pages/
    └── ……引擎候选、操作记录与缓存
~~~

这是逻辑结构，具体文件粒度可按报告规模简化；没有相应内容时不制造大量空占位文件。

日常只需打开“00-阅读入口.md”，其中明确：

- 报告身份、页数、解析版本和整体状态。
- 章节、主表、关键图表的入口。
- 检出的表图数量、结构恢复数量、可计算数据范围。
- 尚未解决的问题及其影响。
- 原 PDF 与复核入口。

正文按章节保存为一份主阅读语料，不再同时放一份完整 report.md 和全部章节供递归重复读取。全文单文件版可作为显式导出，放在独立导出目录。

三种建议运行方式：

- 标准解析：全书登记，正文恢复、表格结构、图表留存、重点标签及质量检查。
- 核心财务整理：从已解析包中确认主表与指定指标，生成可计算事实；不要求重跑全 PDF。
- 局部复核：只处理指定页、表、图或问题，沿用原始证据并生成新版本结果。

用户可以一句话要求“解析这份年报”进入标准方式；接口细节无需暴露给普通使用者。

## 16. 与现有 Skill 的衔接

建议保留 annual-report-markdown 作为用户入口，可在显示名中改为“年报解析与研究包”。文件夹是否改名不是首轮工程重点，也不必同时维护两个功能重叠的 Skill。

现有 annual-report-research 的事实来源限定为 Markdown，且要求保留原值、不换算。不能因为新包增加了标准化数据，就假定旧 Skill 会自动读取并正确使用 JSON。[现有精华提取规则](/Users/eric/.codex/skills/annual-report-research/SKILL.md:13)

两步衔接：

1. V0.2 提供独立 Markdown 兼容导出，只包含一份阅读语料和原值表格；简单表用管道表，已核实的复杂表以 HTML table 内嵌在 Markdown 中，不能只链接外部 HTML。未结构化区域保留原图链接和状态，但图片不能作为旧 Skill 已读到的源表。
2. 后续单独升级精华 Skill，让它理解新包的证据、质量状态和表格 JSON。不要把当前只检查 Markdown 一致性的校验器误当作 PDF 正确性证明。

自动衔接前，由入口程序检查兼容包准入条件：核心主表及本次精华范围所需内容必须可读取，必要语义和结构问题已解决。若核心主表仍只有图片，标为“不满足完整精华提取输入条件”，不自动调用旧 Skill 生成并宣称完整精华。仅给兼容包增加一个说明文件不会使旧校验器自动理解这个限制；升级旧 Skill 之前，由新入口负责执行准入检查。

为旧 Skill 导出的原值仍按原值规则处理；normalized_value 留在新包的数据层。解析整理、精华选择、研究写作三层职责保持清楚。

## 17. 工程实施方式与可维护性

Skill 文本负责输入确认、调用流程、异常选择和交付说明。大量坐标算法、表格恢复及数据校验应由可测试程序实现，不塞进几百条自然语言提示。

计划模块：

~~~text
intake         输入、哈希、身份与体检
geometry       页面、区域和坐标变换
adapters       第三方引擎到统一结构
layout         内容分区与阅读顺序
sections       目录、章节和附注映射
tables         网格、单元格、跨页逻辑表
figures        图表证据与标签
semantics      期间、主体、单位和指标候选
validation     覆盖、结构、语义与财务校验
exports        Markdown、HTML、JSON、CSV及复核页
~~~

主要输入输出先形成版本化 Schema；一个模块不能绕过 Schema 直接修改另一个模块的结果。利用成熟库处理通用布局，我们主要开发适配、证据、财务约束和验收。

运行要求：

- 分页或分区域处理，控制内存；跨页拼表在文档阶段汇总。
- 保存阶段性结果，单页失败不使已完成页面丢失。
- 可恢复运行与局部重试，完成前不能只因进程返回 0 就标成功。
- 缓存键包含源哈希、区域、引擎/模型版本和配置；换模型不能误用旧结果。
- 最终产物先在新运行目录完整生成，验证后再确定为本轮结果；不覆盖旧包。
- 哈希在运行开始与结束核对；报告变化时中止混用不同版本。
- 记录时间、峰值内存、处理页数、模型调用量和失败原因。
- 人工修订独立保存，重跑要明确保留、迁移或提出冲突。

大模型用于候选分类、困难区域理解和语义映射。所有可见披露内容均作为输入数据，不作为改变任务流程的指令；财务值必须回到证据对象，不接受模型凭记忆补齐。

不在首版引入向量库、图数据库、全市场下载服务或自动跨报告合并。文件索引足够支持当前工作流，将来可以在稳定 Schema 上增建检索服务。

## 18. 验收基准：先有真实答案，再谈覆盖率

### 18.1 样本组织

建议先做 12 份报告的选型集，其中现有茅台、格力、中国铝业、中国平安半年报可作为起点，再加入繁体、英文、双语、非自然年和扫描/复杂页面。至少精标 120–180 页，含连续跨页片段；同时跑整份文档检查目录和跨页行为。

主路线确定后，建立约 30–36 份真实报告的首轮基准库，A/H大致均衡，包含完整年报和中期报告。按发行人及版式家族划分开发、调参和冻结测试集；同公司相邻年份不能同时进入开发与测试造成虚高。

如果这一规模尚不能支撑某类准确率结论，继续扩大对应样本，不根据目标倒推“已足够”。后续宣称大范围适用前，增加不同披露年份及未见版式的滚动回归测试。

| 维度 | 必须覆盖的样本 |
|---|---|
| 市场与语言 | A 股简体、港股繁体、港股英文、并排双语、独立中英版本 |
| 行业 | 非金融常见行业、银行、保险、证券、综合企业 |
| 期间 | 自然年、非 12 月年结、中期、季度当季与累计、非标准长度期间 |
| 口径 | 合并/公司、重述前后、准则差异、调整后指标、不同币种 |
| 表格 | 有线/无线、多级表头、横表、跨页表、横向拆表、中途更换表头 |
| PDF质量 | 原生、扫描、混合、隐藏 OCR、乱码、旋转和裁切 |
| 其他内容 | 图表脚注、流程图、勾选框、页脚重启编号、双页扫描 |
| 版本 | 更正版、中英版本、A/H 对应文件、相邻年度口径变化 |

真实案例优先；可控合成样本用于稳定复现旋转、页序、低清、同页多节、金额符号等错误，不用合成结果冒充真实报告覆盖。

### 18.2 人工基准内容

对选定页面标注正文块及顺序、章节起点、表格区域、网格和合并关系、单元格原值、表头路径、单位期间主体、脚注关联、跨页关系和图表证据。

首轮至少覆盖 50–100 张代表性表格及足够多的关键财务单元格。高风险样本由第二人或独立审核流程复查标注分歧；单一模型自动生成的答案不能充当金标准。

### 18.3 分层指标和拟议验收门槛

以下是工程验收建议，不是当前性能、承诺或已测结果。

| 指标 | 测量方式 | 拟议门槛或发布规则 |
|---|---|---|
| 页面登记 | 与 PDF 页清单核对 | 100%，每页有明确完成/失败状态 |
| 已检出对象去向 | 检查处理链及导出引用 | 100%，不允许无记录丢弃 |
| 关键表漏检 | 与人工标注区域对比 | 冻结测试集核心主表无已知漏表；其他表另报召回 |
| 原值与上下文 | 数字、符号、行名、列头、期间、主体、单位联合精确匹配 | 自动接受核心数据争取至少 99.9% 精确率；任何已知关键错配阻止发布 |
| 正文 | 字符错误率和段落/块顺序分别统计 | 冻结集不得重现整页倒序、整段消失、关键否定丢失 |
| 跨页拼接 | 与人工标注逻辑表核对 | 错拼关键表为发布阻断；保守不拼计入待复核率 |
| 证据回查 | 检查值及其上下文字段能定位原 PDF | 所有自动接受核心数据 100% 有足够证据 |
| 人工复核负担 | 待复核对象/全部应处理对象 | 必须公开，不以大量拒绝掩盖精度不足 |
| 性能与成本 | 全书耗时、每页P50/P95、峰值内存、调用费用 | 按本机和真实样本实测，不预报未经测试的秒数 |

99.9% 是目标，不是用几个示例就能证实的结论。必须报告样本数、拒绝/接受比例、各分层错误及统计不确定性；同一表内的许多格子并非独立样本，置信区间应考虑报告/表格聚集。数据不足时写“观察到的结果”，不写总体准确率已达到目标。

“覆盖大部分报告”应同时披露文档留存率、结构化率、可计算覆盖和复核率。不同语言、行业及扫描类型单独汇报，不能用简单中文正文的高分冲淡港股复杂表格的错误。

### 18.4 首批固定回归用例

- 本地茅台第 2 页：一到十一的顺序正确，勾选和标题不散到页尾。
- 本地茅台第 5 页：主要会计数据和指标的期间、元/股、百分比、百分点不混淆。
- 本地茅台第 8 页：识别为正文加表格，所有金额和说明有去向，无双栏分支漏字。
- 本地茅台同页多节：第一、第二节及第七、第八节都能保留。
- 本地茅台“合计”：仍属于相应表格，页眉处理不误删。
- 跨页财务表：保留前页表头、续页数据和合并/公司边界。
- 港股同标题 continued：正确区分下一年度或另一计量口径的子表。
- 单位缺失、负数基期、空白/短横线：不补单位、不补零、不强制套同比公式。

## 19. 分阶段落地，第一阶段就交付完整链路

不再沿用“先做文字，后加结构，最后才做可靠性”的安排。每阶段都有证据与质量门槛，版本增长代表覆盖扩大。

| 阶段 | 交付内容 | 进入下一阶段的条件 |
|---|---|---|
| P0：方案后选型验证 | 标注样本、候选引擎对比、统一结构最小Schema、实际资源报告 | 明确主路线和困难页处理方式，确认必要证据能取得 |
| V0.2：最小完整研究包 | 正文MD、原始证据、表格JSON/HTML、图表裁剪与标签、章节/脚注、核心事实候选、质量与复核入口；版本登记、独立运行目录、满足准入条件的Markdown兼容导出 | 通过首批真实报告及已知错误回归；未解决问题可定位；来源及引擎版本可复现 |
| V0.3：扩大A/H覆盖 | 繁英双语、无框线及复杂跨页、完整主表类别、准则和行业配置、更多图表标签 | 按市场/行业/语言的冻结测试，覆盖与复核率可量化 |
| V1.0：稳定可维护 | 完善局部恢复、模型升级兼容、人工修订迁移、复核体验及回归集 | 达到明确支持矩阵，文档、结构和财务数据质量分别可报告 |

V0.2 允许某些困难表只能输出原图和待复核状态，但不能以“暂不支持表格”为由完全缺席该分支。核心财务先覆盖主表及明确的指标，不要求一开始统一所有附注科目。

V0.2 已需保存源哈希、程序/模型/配置版本，并采用不覆盖的运行目录；可先只支持完整重跑和独立局部重跑，不承诺复杂增量合并。V1.0 完善的是运行恢复与迁移能力，而不是到那时才开始记录证据和版本。

最先验证的技术难点是可靠字符位置、复杂表格网格和期间/单位绑定。若候选主引擎在这些方面不合格，P0 就更换路线；不等写完大量 Skill 指令才承认底层不可用。

工作量的主要部分在表格、财务上下文、样本标注和回归验证。完成 P0 后依据真实缺口估算工期和机器资源，当前不把它描述成修改几个正则就能完成的小升级。

## 20. 本方案建议确定的决策

1. 采用“统一文档结构 + 多格式研究包”，替换当前纯文本底座。
2. 原值、证据、变换记录和质量状态从第一版就保存。
3. 表格以结构 JSON 和 HTML 为主，CSV 是派生计算视图。
4. 图表先完整留存和可核对标签，再逐步增加理解深度。
5. 财务标准化基于完整上下文，可明确保留未知，不强制同义映射。
6. 复用成熟解析引擎，主引擎先由代表性 A/H 样本选型。
7. 用户只面对清楚的阅读入口与复核清单，复杂中间文件不要求日常打开。
8. 先完成 P0 和一个有证据、有表图、有验收的 V0.2，再扩大支持范围。

本次实际完成的是旧方案审查、代码与产物核对、官方资料调研和这份新设计稿。新解析器、引擎安装、旧产物替换以及 Skill 更新均尚未执行。
